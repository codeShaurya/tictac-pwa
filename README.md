This Game is build with React, Redux, Node Js, Docker, Material UI.
To run this App:
Clone this repo:
naviagate to the repo

and type: 
        Only First time
        sudo docker build -t tictac .
        
        sudo docker run tictac  